## 🐳 Phase 2: Container Manager Prompts (6–10)

### Prompt 6: Create the Docker Container Manager
(Create `orchestrator/container_manager.py` with the full spec shared)

### Prompt 7: Create the Base Docker Image
(Create `docker/Dockerfile.cursoft-dev` and `docker/build.sh` as shared)

### Prompt 8: Create the Container Orchestrator
(Create `orchestrator/container_orchestrator.py` as shared)

### Prompt 9: Create Integration Tests for Containers
(Create `tests/test_container_manager.py`, `tests/test_container_orchestrator.py`, optional `tests/integration/test_docker_integration.py` as shared)

### Prompt 10: Create the Container Demo Script
(Create `demo_containers.py` as shared)
